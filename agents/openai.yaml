interface:
  display_name: "SylixOS Dev"
  short_description: "搭建 SylixOS 环境，并管理工程、上传与 telnet 调试"
  default_prompt: "使用 $sylixos-dev 帮我创建或复用一个 SylixOS sydev 环境，或在 upload 后按设备配置做 telnet 调试。"

policy:
  allow_implicit_invocation: true
